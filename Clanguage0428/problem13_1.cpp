
#include <stdio.h>
// 표준 입출력 함수를 사용하기 위해 포함

int main(void)
{
    int arr[5] = { 1, 2, 3, 4, 5 };
    // 크기가 5인 int형 배열을 선언하고 초기화하려 했으나,
    // 초기화 값이 6개(0~5)라서 경고 또는 오류가 발생할 수 있음.
    // 배열 크기와 초기화 값의 개수가 일치해야 함.
    // 정확히 하려면 크기를 6으로 수정하거나 초기화 값을 5개로 줄여야 함.

    int* ptr = arr;
    // 포인터 ptr을 배열 arr의 첫 번째 요소를 가리키도록 설정

    int i;

    // 배열의 각 요소에 2를 더하는 반복문
    for (i = 0; i < 5; i++)
    {
        *ptr += 2;  // 현재 포인터가 가리키는 배열 요소에 2를 더함
        ptr++;      // 다음 배열 요소를 가리키도록 포인터를 증가시킴
    }
    /* *ptr → arr[0](1) 에 2를 더해서 3이 됨 → arr = [3, 2, 3, 4, 5]

        * ptr → arr[1](2) 에 2를 더해서 4가 됨 → arr = [3, 4, 3, 4, 5]

        * ptr → arr[2](3) 에 2를 더해서 5가 됨 → arr = [3, 4, 5, 4, 5]

        * ptr → arr[3](4) 에 2를 더해서 6이 됨 → arr = [3, 4, 5, 6, 5]

        * ptr → arr[4](5) 에 2를 더해서 7이 됨 → arr = [3, 4, 5, 6, 7]
    
    

    // 변경된 배열 요소들을 출력하는 반복문
    for (i = 0; i < 5; i++)
        printf("%d ", arr[i]);  // 각 배열 요소를 출력 (띄어쓰기로 구분)

    return 0;  // 프로그램 종료
}


